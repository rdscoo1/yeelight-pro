[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = -q -p no:cacheprovider
markers =
    no_patch_setup: disable the opt-in setup_entities patch fixture
    patch_setup_entities: replace XDevice.setup_entities with a no-op for unit tests
